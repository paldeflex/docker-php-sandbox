{
    "name": "docker-php-sandbox/app",
    "description": "Docker PHP Sandbox application",
    "type": "project",
    "autoload": {
        "psr-4": {
            "App\\": "./"
        }
    },
    "require": {
        "php": ">=8.5"
    },
    "config": {
        "sort-packages": true
    }
}